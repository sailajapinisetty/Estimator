{
  "name": "ai-cost-estimator-backend",
  "version": "1.0.0",
  "description": "Backend API for AI cost estimator",
  "main": "server.js",
  "scripts": {
    "predev": "lsof -ti:5000 | xargs kill -9 2>/dev/null || true",
    "prestart": "lsof -ti:5000 | xargs kill -9 2>/dev/null || true",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": ["embeddings", "tokens", "cost"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "js-tiktoken": "^1.0.7",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
